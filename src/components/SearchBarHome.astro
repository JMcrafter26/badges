---
// SearchBar component for homepage that redirects to badges page
---

<div class="search-container-home bg-gradient-to-br from-blue-50 to-indigo-50 dark:from-zinc-800 dark:to-zinc-900 rounded-xl p-8 shadow-lg border border-blue-100 dark:border-zinc-700">
	<div class="max-w-5xl mx-auto">
		<h2 class="text-zinc-800 dark:text-zinc-100 mb-2 text-xl font-bold">Find Your Perfect Badge</h2>
		<p class="text-zinc-600 dark:text-zinc-400 mb-5 text-sm">Search through hundreds of badges in multiple styles and formats</p>
		
		<div class="search-wrapper">
			<input 
				type="text" 
				id="badge-search-home" 
				placeholder="Search badges by name..." 
				class="w-full px-4 py-3 text-base border-2 border-blue-200 dark:border-zinc-600 rounded-lg 
				       bg-white dark:bg-zinc-800 text-zinc-900 dark:text-zinc-100
				       focus:outline-none focus:border-blue-500 dark:focus:border-blue-400
				       placeholder-zinc-400 dark:placeholder-zinc-500 shadow-sm
				       cursor-pointer hover:border-blue-400 dark:hover:border-blue-500 transition-colors"
				readonly
			/>
			
			<div class="mt-3 text-xs text-zinc-500 dark:text-zinc-500 text-center">
				Click to start searching • Navigate by badge type or search directly
			</div>
		</div>
	</div>
</div>

<script>
	// Redirect to badges page when search is clicked
	const searchInput = document.getElementById('badge-search-home');
	
	if(searchInput) {
		searchInput.addEventListener('click', () => {
			window.location.href = '/badges/cozy';
		});
		
		// Also handle focus
		searchInput.addEventListener('focus', () => {
			window.location.href = '/badges/cozy';
		});
	}
</script>

<style>
	.search-container-home {
		transition: transform 0.2s ease, box-shadow 0.2s ease;
	}
	
	.search-container-home:hover {
		transform: translateY(-2px);
		box-shadow: 0 12px 24px rgba(0, 0, 0, 0.1);
	}
	
	.dark .search-container-home:hover {
		box-shadow: 0 12px 24px rgba(0, 0, 0, 0.3);
	}
</style>
